<!--This is just in case you hit any errors when creating a new article-->
<!--These messages come from the validation restrictions you set up in your article.rb file-->

<% if obj.errors.any? %>
  <div class="alert alert-danger alert-dismissible fade show" role="alert">
<!--    This grabs the class name (whether it be article, user, etc) and puts it in the alert heading-->
    <h4 class="alert-heading">The following errors prevented the <%= obj.class.name.downcase %> from being saved</h4>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>